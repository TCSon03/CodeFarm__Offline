let myName = "Son";

console.log(myName);

// không thể thêm, cập nhật, xóa 1 phần trong chuỗi

for(let i = 0; i < myName.length; i++){
    console.log(myName[i]);
}

// chuyển đổi sang kiểu chuỗi 
let age = 100;
console.log(age.toString());
console.log((age + ""));


// Biến đổi chuỗi
let schools = "class CODEFARM";
console.log(schools.toUpperCase());
console.log(schools.toLowerCase());

console.log(schools);

